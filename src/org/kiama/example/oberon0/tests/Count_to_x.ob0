(* Enter a number and see the numbers between 1 and that number *)

MODULE CountToX;
    VAR i, stopval : INTEGER;

BEGIN

    Read (stopval);
    i := 1;

    WHILE i <= stopval DO

        WriteLn (i);
        i := i + 1

    END

END CountToX.
