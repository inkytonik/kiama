(* Procedure testing *)

MODULE ProcTest2;

    PROCEDURE Proc1 (a, b : INTEGER);
    
        PROCEDURE Nested1 (c, d : INTEGER);
        BEGIN
            Proc1 (1, 2);		(* Invalid proc call:  Call must be to child or sibling *)
            Nested1 (3, 4);
            Nested2 (5, 6)
	    END Nested1;        

        PROCEDURE Nested2 (d, f : INTEGER);
        BEGIN
	    END Nested2; 

	BEGIN
	    Nested1 (1, 2)
	END Proc1;

BEGIN

    Proc1 (3, 4);
    Nested1 (5, 6)		(* Invalid proc call:  Call must be to child or sibling *)

END ProcTest2.
