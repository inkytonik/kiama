    ! Prologue
    movi $27, $0, 0
    ! StW(Local(0),Read())
    rd $1
    stw $1, $27, 0
    ! Write(SequenceDatum(List(StW(Local(4),IntDatum(4)), Bne(LdW(Local(4)),Label(1)), StW(Local(8),IntDatum(0)), Jmp(Label(2)), LabelDef(Label(1))),RemW(IntDatum(15),LdW(Local(4)))))
    ! StW(Local(4),IntDatum(4))
    movi $1, $0, 4
    stw $1, $27, 4
    ! Bne(LdW(Local(4)),Label(1))
    ldw $1, $27, 4
    cmpi $1, 0
    bne label1
    ! StW(Local(8),IntDatum(0))
    movi $1, $0, 0
    stw $1, $27, 8
    ! Jmp(Label(2))
    br label2
    ! LabelDef(Label(1))
label1:
    movi $1, $0, 15
    ldw $2, $27, 4
    mod $1, $1, $2
    wrd $1
    wrl
    ! Ret()
    br label3
    ! Write(IntDatum(0))
    movi $1, $0, 0
    wrd $1
    wrl
    ! Ret()
    br label3
    ! LabelDef(Label(2))
label2:
    ! Write(IntDatum(-1))
    movi $1, $0, -1
    wrd $1
    wrl
    ! Epilogue
label3:
    ret $0
